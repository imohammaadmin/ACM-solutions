#include <iostream>
#include <vector>
#include <string>
#define MohammadAmin main()
using namespace std;


int MohammadAmin{
    string s;
    vector<int> n;
    vector<char> x;
    int a = 0, b = 0;
    bool isPossible = false;
    cin >> s;
    for ( int i = 0; i < s.size(); i++ ){
        if ( s[i] == '#' ){
            
        }else{
            x.push_back( s[i] );
        }
    }
	return 0;
}
